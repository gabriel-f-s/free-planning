services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=freeplanning'
      - 'POSTGRES_PASSWORD=${SUN_DATABASE_PASSWORD}'
      - 'POSTGRES_USER=${SUN_DATABASE_USERNAME}'
    ports:
      - '5435:5432'
